function createGrid() {
    const main = document.createElement('main');

    for (let i = 0 ; i < 16 ; i++) {
        const div = document.createElement('div')
        main.appendChild(div);
    };
};